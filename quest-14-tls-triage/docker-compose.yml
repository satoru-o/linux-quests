services:
  server:
    build: ./server
    container_name: quest14-server
    hostname: ip-10-0-5-14
    privileged: true
    cgroup: host
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    extra_hosts:
      - "reports.internal:127.0.0.1"
    ports:
      - "2226:22"

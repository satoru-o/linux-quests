services:
  server:
    build: ./server
    container_name: quest10-server
    hostname: ip-10-0-1-42
    privileged: true
    cgroup: host
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    ports:
      - "2222:22"

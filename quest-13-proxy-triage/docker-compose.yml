services:
  server:
    build: ./server
    container_name: quest13-server
    hostname: ip-10-0-4-25
    privileged: true
    cgroup: host
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    ports:
      - "2225:22"

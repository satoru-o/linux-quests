# 保守用の接続情報 (運用担当のみ)
# libpq の環境変数名にしてあるので、export すれば psql に引数は要らない。
#
#   export $(sudo grep -v '^#' /opt/reportdb/maint.conf | xargs)
#   psql
#
PGHOST=reportdb.internal
PGPORT=5432
PGDATABASE=reportdb
PGUSER=dbadmin
PGPASSWORD=dbadmin_pw

# 経路が塞がっているときに延々と待たされないように
PGCONNECT_TIMEOUT=5
